{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos mínimos de la ventana principal. Cada permiso está aquí porque la aplicación lo usa; no hay ninguno 'por si acaso'.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "opener:allow-open-url",
    "dialog:allow-message",
    "dialog:allow-confirm"
  ]
}
